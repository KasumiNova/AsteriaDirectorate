package cn.kasuminova.astd.renderer.effect.projectile.beam

/**
 * 原 `AttachedBeamFlareRingRenderer`（FlareEntity 版）已移除：该实现容易产生明显“圆盘/贴片感”。
 *
 * 现在使用 `AttachedBeamSpriteRingRenderer`（SpriteEntity + 运行时生成的空心圆环贴图）。
 */
